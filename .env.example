# 本机直连 PostgreSQL / Redis（或 `docker compose up -d db redis` 后连 localhost）
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/replyboost?schema=public"
JWT_SECRET="change_me_in_prod"
REDIS_URL="redis://127.0.0.1:6379"

# 使用 `docker compose up` 全栈时，Compose 已为 web/worker 注入连接串，无需本文件。
