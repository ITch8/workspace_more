export default function HomePage() {
  return (
    <section className="space-y-3">
      <h1 className="text-2xl font-semibold">ReplyBoost Sprint 1</h1>
      <p>
        MVP baseline is ready for auth, campaign CRUD, CSV lead import, and
        lead query.
      </p>
    </section>
  );
}
